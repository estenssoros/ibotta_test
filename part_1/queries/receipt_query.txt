INSERT INTO receipts (
    customer_id,
    validation_type,
    total,
    processing_state,
    validations,
    capture_lat,
    capture_long,
    created_at,
    store_id,
    retailer_id,
    turk_submit_count,
    invalid_receipt,
    ref_number,
    appr_code,
    receipt_code,
    cashier,
    payment_trans_id,
    phone_data,
    verified_total,
    pending_acceptance_at,
    pending_credit_at,
    store_number,
    receipt_scan_content,
    fraud_type)
VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s);
